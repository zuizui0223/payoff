# PAYOFF residual programme after SLK integration

The integrated flagship hierarchy is now canonical in [`zuizui0223/slk`](https://github.com/zuizui0223/slk).

SLK imports only the minimal PAYOFF transport needed for the flagship:

```text
Phi
-> local accessibility
-> rare invasion
-> fixation
-> weak-mutation occupancy.
```

PAYOFF remains canonical for the mathematical extensions beyond that minimal transport.

## Residual paper A — continuous and topological architecture

Working thesis:

**From partial modularity to branching and discrete architecture.**

Develop:

- continuous recovery coordinate `r`;
- interior architecture optimum `C'(r*)=1`;
- branching threshold under frequency-dependent feedback;
- endpoint coexistence connection;
- edgewise recovery derivative;
- convex recovery and vertex/topology results;
- general symmetric architecture-pair canonicalization where it extends beyond SLK.

## Residual paper B — spatial and temporal transport

Working thesis:

**Spatial and temporal transport of architecture payoff.**

Develop:

- spectral metapopulation invasion from local margins;
- source-sink rescue and migration thresholds;
- common additive temporal forcing as a null case;
- source switching and noncommuting seasonal operators;
- exact `asinh` solution;
- finite migration optimum `m tau -> 1.6061152988...` in weak contrast;
- temporal premium and coordination-barrier crossing.

## Material retained but subordinate

The global-value versus local-accessibility barrier remains fully derived here, but SLK may use its minimal form as a flagship bridge between `Phi>0` and evolutionary realization.

## Scope boundary after SLK

PAYOFF should no longer need to carry the organismal conflict-identification and architecture-value story in full. Its independent identity becomes deeper mathematical theory of architecture dynamics after the SLK handoff.